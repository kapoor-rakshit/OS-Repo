
# REFERENCE : https://www.computerhope.com/unix/curl.htm

# cURL
# curl is a tool to transfer data from or to a server, using one of the supported protocols (HTTP, HTTPS, FTP, FTPS, SCP, SFTP, TFTP, DICT, TELNET, LDAP or FILE).
# The command is designed to work without user interaction.
# It offers proxy support, user authentication, FTP uploading, HTTP posting, SSL connections, cookies, file transfer resume, Metalink etc.

curl <URL>

